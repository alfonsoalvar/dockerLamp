http:
  middlewares:
    dashboard-auth:
      basicAuth:
        users:
          - "admin:$apr1$n2ddcHzO$2pJbQEAspBlC8/XqNh2ta/" # usuario y clave 

  routers:
    traefik-dashboard:
      # Regla del dashboard
      rule: "Host(`traefik.local`)"
      service: api@internal
      middlewares:
        - dashboard-auth
      entryPoints:
        - websecure
      tls: {}
